{
  "name": "voice-measurement-sheet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "工程能力測定用 音声入力測定シート",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "idb": "^8.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
